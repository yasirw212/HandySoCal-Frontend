import { createSlice } from "@reduxjs/toolkit";

const options = {
    name: 'contact',
    initialState:{
        formActive
    },
    reducers:{
        toggleForm: (state, {payload}) =>{

        }
    }
}